package cmd

// The shared command builders (newTopCmd, newGetCmd, newDailyCmd,
// newSummaryCmd, dailyHook) that used to live here now live in
// report_shared.go so both the ks and oe trees can reuse them.
